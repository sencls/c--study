#include <iostream>
#include <vector>
#include <utility>
#include <vector>
using namespace std;
int main()
{
    vector<int> ans;
    ans.resize(10);
    for (int i = 0; i < ans.size(); i++)
    {
        cout << ans[i] << endl;
    }
    return 0;
}